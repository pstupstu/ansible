---
- name: Restart cron
  ansible.builtin.service:
    name: cron
    state: restarted

- name: Systemd_daemon_reload
  ansible.builtin.systemd:
    daemon_reload: true

- name: sshd_restart
  ansible.builtin.systemd:
    name: ssh
    state: restarted
